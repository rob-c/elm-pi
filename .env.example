# Copy to .env (mode 600) and fill in. .env is gitignored; never commit the key.
# ELM keys are issued on request through the ELM web UI - see INSTALL.md step 1.
ELM_API_KEY=

# Optional overrides
# ELM_BASE_URL=https://elm.edina.ac.uk/api/v1
# ELM_SHIM_PORT=8811
